# Experiment 阶段的子流形路由

读取上游项目向量，并将每条活动轴转译为实验负担：

- E：形态/接触干预、制造批次和接触失败；
- P：传感器标定、留出对象/场景、不确定性与误检；
- C：动力学/稳定性对照、扰动、恢复时间和安全边界；
- L：数据 lineage、seed/split/checkpoint、迁移与消融；
- D：规划基线、长时域任务、协同规模和失败归因；
- H：参与者/操作者层级、伦理、协议、效应量和工作负荷；
- A：运行包络、持续运行、跨环境/站点和失效恢复；
- S：版本、接口、频率、资源、日志和可重跑 artifact。

把这些转成 Design Lock 的 condition/contrast/metric/analysis；不得因为 venue 更喜欢某种实验而事后修改单位、分母或排除规则。
